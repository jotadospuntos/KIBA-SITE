# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
next-env.d.ts
*.tsbuildinfo

# local-only visual scratch preview (never deploy)
/public/borderglow-preview.html
/public/borderglow-diagnostic.html

# Full-size source images (originals for public/img/hero/*.webp). Kept locally so
# the optimized versions can be regenerated, but not committed - the 9 Pexels
# originals are ~24MB and would live in git history forever.
/assets-src/
